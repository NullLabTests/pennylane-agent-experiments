id: H4
title: "PDE-Constrained Loss Functions Suppress Gradient Vanishing"
description: >
  Embed PDE constraints into VQC loss functions and compare gradient variance
  decay rates against global and local cost baselines across qubit counts.
device:
  backend: default.qubit
  wires: 6
  shots: null
hyperparams:
  qubit_range: [2, 3, 4, 5, 6]
  n_samples: 80
  alpha: 0.1
  epsilon: 0.01
  seed: 42
expected_runtime_sec: 120
resource_caps:
  max_qubits: 6
  max_gates: 500
  max_shots: null
